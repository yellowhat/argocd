---
# First installation prometheus CRDs are not installed
prometheusRule:
  enabled: false

argo-cd:
  controller:
    metrics:
      serviceMonitor:
        enabled: false

  redis:
    metrics:
      serviceMonitor:
        enabled: false

  repoServer:
    metrics:
      serviceMonitor:
        enabled: false

  server:
    metrics:
      serviceMonitor:
        enabled: false
